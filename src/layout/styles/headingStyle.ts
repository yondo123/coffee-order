import styled from '@emotion/styled';

export const XXLHeading = styled.h1`
  font-size: 24px;
  line-height: 34px;
  letter-spacing: -0.01em;
  font-weight: 700;
`;

export const XLHeading = styled.h2`
  font-size: 18px;
  line-height: 28px;
  letter-spacing: -0.02em;
  font-weight: 700;
`;

export const LargeHeading = styled.h3`
  font-size: 16px;
  line-height: 24px;
  letter-spacing: -0.01em;
  font-weight: 700;
`;

export const MediumHeading = styled.h4`
  font-size: 13px;
  line-height: 20px;
  letter-spacing: -0.01em;
  font-weight: 700;
`;

export const SmallHeading = styled.h6`
  font-size: 12px;
  line-height: 16px;
  letter-spacing: -0.005em;
`;
